name: Replica smoke

on:
  workflow_dispatch:
  schedule:
    - cron: '17 2 * * *'

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: scripts/smoke-replicas.sh
