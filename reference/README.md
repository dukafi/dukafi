Reference only. Do not run or import.

`instatic/` is a full upstream clone of https://github.com/CoreBunch/Instatic.git,
kept with its own `.git` so it can be `git fetch`-ed to diff against upstream. It
is gitignored — never tracked as part of this repo. See `dukafy/routes/admin_api.rb`
for the Ruby reimplementation of its server, and `dukafy-editor/` for Dukafy's
tracked fork of its editor client.
